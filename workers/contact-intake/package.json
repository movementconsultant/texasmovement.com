{
  "name": "tmi-contact-intake-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Cloudflare Worker for the texasmovement.com contact form — not part of the Astro site build or its dependency tree.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.9.0",
    "wrangler": "^3.90.0"
  }
}
