# PR review screenshots

Before/after screenshots captured for the `claude/texas-movement-rebuild-pq14fo` PR, referenced
from the PR description. Captured with the pre-installed Playwright/Chromium
(`/opt/pw-browsers/chromium`) at matching viewports:

- `old-home-*` — the OLD `legacy/index.html` (with its images copied alongside it so it renders
  faithfully), served from a plain static file server.
- `new-home-*` / `new-lanes-*` — the NEW Astro build (`astro preview`).

Safe to delete after the PR is reviewed/merged — these aren't part of the site, just review
artifacts.
