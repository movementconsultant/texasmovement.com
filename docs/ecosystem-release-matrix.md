# TMI ecosystem release matrix

Snapshot as of the Mark 2 ecosystem-wide release sprint. Covers every repository the
`movementconsultant` GitHub organization actually contains — confirmed via a live repository
listing at the start of this sprint: **exactly 10 repositories exist**, no more. Any property
without its own row-worthy repo (HERO) is recorded here rather than skipped.

Each vertical's own `docs/mark-2-vertical-audit.md` (in its own repository) has the full detail
behind its row here — this table is the cross-ecosystem summary, not a replacement for those.

| Vertical | Repository | Domain/hostname | Current classification | CI/build status | Real destination | Link/contact status | Launch recommendation | Owner blocker | Next action |
|---|---|---|---|---|---|---|---|---|---|
| TMI Umbrella | `texasmovement.com` | texasmovement.com | **Live candidate** | PASS — full `npm run ci` gate (build, `check-public-output.mjs`, `check:constants --strict`, 31/31 unit tests, axe-core 0 violations/5 routes), re-verified in production mode this sprint | None yet — not deployed anywhere | No verified contact inbox; no verified social links; ecosystem footer correctly empty | Ready for controlled launch after owner verification | Cloudflare Pages project/domain binding; `hello@texasmovement.com` 3-part operational verification; LinkedIn Company Page URL confirmation | Owner approves production release packet (see `docs/mark-2-production-release-report.md`) |
| Founder Control Tower | `alexandermathai.com` | alexandermathai.com | **Live candidate** | PASS — `astro check` 0 errors, `prettier --check` clean, production build 17 pages, axe-core 0 violations/9 routes | None yet — not deployed anywhere | No verified contact endpoint; all 11 social/ecosystem URLs `verified: false`, rendered as plain text | Ready for controlled launch after owner verification | Cloudflare Pages project/domain binding; social URL verification; 7 flagged claims need sign-off | Owner approves production release packet (see `docs/mark-2-production-release-report.md`, that repo) |
| Texas Movement Consulting | `consulting.texasmovement.com` | consulting.texasmovement.com | **Building** | PASS — full CI gate, both preview/production build modes, 9/9 unit tests, axe-core 0 violations/7 routes. One real defect found and fixed this sprint: a stale `/accessibility` claim describing a testimonials iframe that had already been removed | None — `wrangler.toml` scaffolded, no Cloudflare project connected | No verified inbox (`consulting@`/`hello@texasmovement.com`); testimonials Google Form already disabled pending re-verification | Not launch-ready — substantial, clean rebuild but no live deployment | No Cloudflare access; inbox verification; real `/privacy`/`/terms` copy | Remain Building; owner to connect Cloudflare + verify inbox when ready |
| Texas Movement Media | `media.texasmovement.com` | media.texasmovement.com | **Building** | PASS — full CI gate, both build modes, 10/10 unit tests, axe-core 0 violations/7 routes | None — no Cloudflare project connected | `VERIFIED_INBOXES` empty, no contact CTA renders; social links TBD-gated | Not launch-ready — clean rebuild, no live deployment | No Cloudflare access; inbox verification; two conflicting legacy contact addresses need owner resolution | Remain Building; owner to connect Cloudflare + resolve contact address when ready |
| Texas Movement Performance | `performance.texasmovement.com` | performance.texasmovement.com | **Building** | PASS — typecheck, both build modes, 19/19 unit tests, axe-core 0 violations/5 routes | None — live DNS lookup for `performance.texasmovement.com` returned `ENOTFOUND` this sprint, confirming no deployment exists | No CTA, no contact, no social links anywhere in output | Correctly not launch-ready | No Cloudflare/DNS connection; legal review of legacy performance/medical-adjacent claims before any future expansion | Remain Building; no action needed beyond eventual Cloudflare connection |
| Texas Movement Distribution | `distribution` | (no confirmed hostname in code) | **Building** | PASS — typecheck, build, 16/16 unit tests, axe-core 0 violations/5 routes | None — no Cloudflare project connected | No CTA, no contact, no social links | Correctly not launch-ready | No Cloudflare/DNS connection; legacy IP-enforcement copy needs legal review before any future restoration | Remain Building |
| Texas Movement Social | `social.texasmovement.com` | social.texasmovement.com | **Building** | PASS — typecheck, both build modes, 16/16 unit tests; axe-core partial (2/5 routes completed, 0 violations) — sandbox network instability with Google Fonts prevented a full run, not a repo defect | None — no Cloudflare project connected | No CTA, no contact, no social links | Correctly not launch-ready | No Cloudflare/DNS connection; owner decision on legacy "Gather" events framing; full a11y re-run recommended in an unrestricted environment | Remain Building |
| FounderLink | `founderlink.texasmovement.com` | founderlink.texasmovement.com | **Building/Private** | PASS — typecheck, both build modes, 16/16 unit tests, axe-core 0 violations/5 routes | None — no Cloudflare project connected | No CTA, no intake form, no contact route of any kind | Correctly not launch-ready | No Cloudflare/DNS connection; GitHub repo description metadata still reads "Vetted founder network" — a platform-settings mismatch with actual build content, not a code defect | Remain Private; owner should update the repo description field to match actual status |
| Texas Movement Health | `health` | health.texasmovement.com (referenced, not deployed) | **Building/Private** | PASS — typecheck, both build modes, 16/16 unit tests, axe-core 0 violations/5 routes | None — no Cloudflare project connected | No CTA, no email, no forms | Correctly not launch-ready | No Cloudflare/DNS connection; **flagged for a real legal/medical-compliance review** (heavier than the generic stub review) before any future public claim, given subject matter | Remain Private |
| TM Reparations Partners | `reparations.texasmovement.com` | reparations.texasmovement.com | **Building/Private** | PASS — typecheck, build, 16/16 unit tests; a11y scan blocked by this sandbox's network policy (`cdn.playwright.dev` unreachable) — flagged, not worked around | None — no Cloudflare project connected | No CTA, no email, no forms | Correctly not launch-ready | No Cloudflare/DNS connection; **flagged for a real legal/reputational review** (heavier than the generic stub review) before any future public copy beyond the one approved blurb, given subject matter; a11y re-run needed in an unrestricted environment | Remain Private |
| HERO Footwear & Performance | **No repository exists** | hero.texasmovement.com (referenced in `@tmi/constants` only) | **Reserve** | N/A — nothing to build or audit | None | N/A | Not applicable | No accessible GitHub repository for this property — confirmed via a live org repository listing (10 repos total, HERO not among them) | Owner locates or creates the HERO repository before any further work is possible here |

## What this table does not do

It does not represent any vertical as Live/Operating/Available beyond the two hub sites, which are
themselves only "Live candidate" (release-ready, not yet deployed) — not actually live in
production. Every "Building"/"Private" classification above was independently confirmed this
sprint by re-running each repo's own real build/test/CI commands against its actual output, not
assumed from a prior pass's documentation.
